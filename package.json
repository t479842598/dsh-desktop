{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "dsh desktop shell: Tauri 2 壳 + dsh Web UI + 自动更新管线 + 远程访问",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}