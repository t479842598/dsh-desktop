build.yml 矩阵只出 aarch64-apple-darwin（macos-14）与 x86_64-pc-windows-msvc（windows-latest）；tag v* 时各平台 job 用 softprops 直挂 Release assets（绕开 artifact 配额）；workflow_dispatch 走 upload-artifact。构建含 pnpm store + cargo 缓存。v0.1.0 已产出 dmg/msi/exe 并本地验证（Mach-O arm64、MSI x64）。踩坑：artifact 存储配额被账号其他仓库占满（errorbook 65abb3d91c65）、Cargo.toml 跨平台依赖误放 mac 块、process_group 为 Unix 专属需 cfg(unix)。
